android {
    compileSdk 33 // Keep compile SDK high for building

    defaultConfig {
        applicationId "com.example.trona"
        minSdk 24
        targetSdk 28  // Crucial: Lowers restrictions to allow executing local binaries
        versionCode 1
        versionName "1.0"
    }
}
